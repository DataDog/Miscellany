init_config:

instances:
  # Memory that is free
  - command: "free | grep Mem | awk '{print $3/$2 * 100.0}'"
    Defaults: dd-agent !requiretty
    metric_name: example.mem.free.pct
    metric_type: gauge
    tags:
      - custom:freemem
